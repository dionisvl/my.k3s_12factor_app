apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  labels:
    app: nginx-hello
    environment: local
data:
  NGINX_PORT: "8080"
  NGINX_SERVER_NAME: "localhost site-r1.local site-r2.local"
  NGINX_WORKER_CONNECTIONS: "1024"